[pytest]
# Register custom marks so pytest doesn't warn about unknown marks
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    timeout: marks a wall-clock timeout for a test (used with pytest-timeout)
